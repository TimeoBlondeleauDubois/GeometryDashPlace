<aside class="editor-status" aria-live="polite">
    <strong>@Editor.CoordinateText</strong>
    <span>@Editor.ZoomText</span>
    <span>@Editor.ObjectCountText</span>
</aside>

@code {
    [CascadingParameter]
    public required EditorSession Editor { get; set; }
}
